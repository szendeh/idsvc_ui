define([
    'keel/Router'
],
function(Router) {
    'use strict';

    /**
    * Defining the application router, you can attach sub routers here.
    *
    * @class AppRouter
    * @constructor
    * @extends Router
    **/
    var AppRouter = Router.extend();

    return AppRouter;
});